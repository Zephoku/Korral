/*

Multi Application Platform Push system (MAPP)
For: Korral

*/

$(document).ready(function(){

  // listen for drag/drop
  var dropped = ui.draggable.attr('id');
  if ("sq" == dropped){
     alert('sup'); 
  }
})